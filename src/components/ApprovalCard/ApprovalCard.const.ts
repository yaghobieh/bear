import { LABEL_APPROVE, LABEL_REJECT } from '@const';
import type { ApprovalCardTranslations } from './ApprovalCard.types';

export const APPROVAL_CARD_DEFAULT_TRANSLATIONS: ApprovalCardTranslations = {
  approveLabel: LABEL_APPROVE,
  rejectLabel: LABEL_REJECT,
};
