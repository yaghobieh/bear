export const CSS_BASELINE_STYLE_ID = 'bear-css-baseline';

export const CSS_BASELINE_RULES = `
*,*::before,*::after{box-sizing:border-box}
html{-webkit-text-size-adjust:100%;tab-size:4}
body{margin:0;font-family:var(--bear-font-sans,system-ui,sans-serif);font-size:1rem;line-height:1.5;color:var(--bear-text-primary,#111827);background-color:var(--bear-bg-primary,#ffffff)}
`;
