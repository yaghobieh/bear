/** Default font size in pixels */
export const DEFAULT_FONT_SIZE = 16;

/** Default rotation in degrees */
export const DEFAULT_ROTATE = -22;

/** Default opacity */
export const DEFAULT_OPACITY = 0.15;

/** Default gap [horizontal, vertical] in pixels */
export const DEFAULT_GAP: [number, number] = [100, 100];

/** Default offset [x, y] in pixels */
export const DEFAULT_OFFSET: [number, number] = [0, 0];

/** Default z-index */
export const DEFAULT_Z_INDEX = 9;

/** Default font weight */
export const DEFAULT_FONT_WEIGHT = 400;

/** Default light mode color */
export const DEFAULT_COLOR_LIGHT = 'rgba(0, 0, 0, 0.15)';

/** Default dark mode color */
export const DEFAULT_COLOR_DARK = 'rgba(255, 255, 255, 0.12)';

/** Default font family */
export const DEFAULT_FONT_FAMILY = 'system-ui, -apple-system, sans-serif';

/** Line height multiplier */
export const LINE_HEIGHT_MULTIPLIER = 1.5;

/** Canvas extra padding in pixels */
export const CANVAS_EXTRA_PADDING = 40;

/** Canvas width offset for gap */
export const CANVAS_WIDTH_OFFSET = 200;

/** Canvas half divider */
export const HALF = 2;

/** Degrees to radians conversion */
export const DEG_TO_RAD = Math.PI / 180;
