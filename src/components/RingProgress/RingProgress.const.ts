import { ONE_HUNDRED_TWENTY, TWELVE, HALF } from '@const';

export const DEFAULT_SIZE = ONE_HUNDRED_TWENTY;
export const DEFAULT_THICKNESS = TWELVE;
export const RING_VARIANT_FULL = 'full';
export const RING_VARIANT_HALF = 'half';
export const RING_HALF_SWEEP = HALF;
