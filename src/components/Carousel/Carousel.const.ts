/** Default transition duration in ms */
export const DEFAULT_TRANSITION_DURATION = 500;

/** Min swipe distance to trigger navigation (px) */
export const MIN_SWIPE_DISTANCE = 50;

/** Default thumbnail size (px) */
export const DEFAULT_THUMBNAIL_SIZE = 48;

/** Default active color */
export const DEFAULT_ACTIVE_COLOR = 'var(--bear-primary-500)';

/** Arrow button size (px) */
export const ARROW_BUTTON_SIZE = 40;

/** Progress bar height (px) */
export const PROGRESS_BAR_HEIGHT = 3;
