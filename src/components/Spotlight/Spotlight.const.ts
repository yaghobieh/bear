/** Default shortcut key */
export const DEFAULT_SHORTCUT_KEY = 'k';

/** Default placeholder */
export const DEFAULT_PLACEHOLDER = 'Search...';

/** Default nothing found message */
export const DEFAULT_NOTHING_FOUND = 'No results found';

/** Default result limit */
export const DEFAULT_LIMIT = 10;

/** Backdrop z-index */
export const SPOTLIGHT_Z_INDEX = 9999;

/** Top offset for dialog (percentage of viewport height) */
export const DIALOG_TOP_OFFSET = '15vh';

/** Max dialog width in pixels */
export const DIALOG_MAX_WIDTH = 560;

/** Max results height in pixels */
export const RESULTS_MAX_HEIGHT = 400;

/** Focus delay after open in ms */
export const FOCUS_DELAY = 50;
