import { LABEL_OPEN_ARTIFACT } from '@const';
import type { ArtifactCardKind, ArtifactCardTranslations } from './ArtifactCard.types';

export const ARTIFACT_CARD_DEFAULT_TRANSLATIONS: ArtifactCardTranslations = {
  openLabel: LABEL_OPEN_ARTIFACT,
};

export const ARTIFACT_CARD_DEFAULT_KIND: ArtifactCardKind = 'doc';
